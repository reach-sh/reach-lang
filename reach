#!/bin/sh

HERE=$(dirname $(realpath $0))
HS=${HERE}/hs

if [ -d ${HS}/.stack-work ] ; then
    export STACK_YAML=${HS}/stack.yaml
    stack build
    exec stack exec -- reachc $*
else
    exec docker-compose -f ${HERE}/docker-compose.yml run reach $*
fi
