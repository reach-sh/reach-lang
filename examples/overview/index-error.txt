Verifying knowledge assertions
Verifying for generic connector
  Verifying when ALL participants are honest
Verification failed:
  when ALL participants are honest
  of theorem: assert
  msg: "balance zero at application exit"
  at ./index-error.rsh:33:7:application

  // Violation Witness

  const UInt.max = 1;

  const request/68 = "Alice".interact.request;
  //    ^ could = 1
  //      from: ./index-error.rsh:6:12:property binding

  // Theorem Formalization

  const v97 = 0 == (request/68 - (request/68 - 1));
  //    ^ would be false
  assert(v97);

  Verifying when NO participants are honest
Checked 13 theorems; 2 failures (and 1 omitted repeats) :'(
