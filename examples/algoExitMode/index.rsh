'reach 0.1';

const makeMain = (ALGOExitMode) => Reach.App(() => {
  setOptions({ ALGOExitMode });
  const A = Participant('A', {
  });
  init();

  A.publish();
  const M = new Map(UInt);
  commit();

  A.publish();
  M[this] = 5;
  commit();

  A.publish();
  commit();

  exit();
});

export const m1 = makeMain('DeleteAndCloseOutAll_SoundASAs_UnsoundElse');
export const m2 = makeMain('DeleteAndCloseOutASAs');
