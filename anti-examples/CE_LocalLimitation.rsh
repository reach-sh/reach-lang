'reach 0.1 exe';

const A = newParticipant();

function main() {
  A.only(() => {
    var [ i ] = 0;
    invariant(true);
    while (i < 1) {
      const x = i;
      [ i ] = [ i + 1 ];
      continue; }});
  return 0; }
