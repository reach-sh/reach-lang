Verifying knowledge assertions
Verifying for generic connector
  Verifying when ALL participants are honest
Verification failed:
  when ALL participants are honest
  of theorem: while invariant before loop
  at ./smtSortEq.rsh:17:27:invariant

  // Violation Witness

  const UInt.max = 2;

  const x/124 = <loop variable>;
  //    ^ could = 2
  //      from: ./smtSortEq.rsh:9:27:while
  const netBalance/127 = <loop variable>;
  //    ^ could = 0
  //      from: ./smtSortEq.rsh:9:27:while
  const v153 = <map reduction>;
  //    ^ could = 3
  //      from: reach standard library:139:18:application

  // Theorem Formalization

  const v161 = ((netBalance/127 == 0) ? (v153 == x/124) : false);
  //    ^ would be false
  assert(v161);

  Verifying when NO participants are honest
Checked 19 theorems; 2 failures (and 1 omitted repeats) :'(
