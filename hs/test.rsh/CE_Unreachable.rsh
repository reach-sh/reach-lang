'reach 0.1 exe';

const A = participant({x : uint256});

function main() {
  A.only(() => {
    if ( x == 0 ) {
      if ( x > 0 ) {
        interact.here(0); }
      else {
        interact.here(1); } }
    else {
      interact.here(2); } });
  return 0; }
